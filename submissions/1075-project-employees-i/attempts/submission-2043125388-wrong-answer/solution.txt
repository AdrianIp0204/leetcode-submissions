import pandas as pd

def project_employees_i(project: pd.DataFrame, employee: pd.DataFrame) -> pd.DataFrame:
    return project.merge(employee, on='employee_id', how='inner').groupby('project_id')['experience_years'].mean().reset_index().rename(columns={'experience_years':'average_years'})
