import pandas as pd

def latest_login(logins: pd.DataFrame) -> pd.DataFrame:
    return logins.query("'2020-01-01 00:00:00' <= time_stamp <= '2020-12-31 23:59:59'").groupby('user_id').agg({'time_stamp':'max'}).reset_index().rename(columns={'time_stamp':'last_stamp'})
