import pandas as pd

def account_summary(users: pd.DataFrame, transactions: pd.DataFrame) -> pd.DataFrame:
    return transactions.groupby('account').agg({'amount':'sum'}).merge(users,on='account').query("amount > 10000")[['name','amount']].rename(columns={'name':'NAME','amount':'BALANCE'})
