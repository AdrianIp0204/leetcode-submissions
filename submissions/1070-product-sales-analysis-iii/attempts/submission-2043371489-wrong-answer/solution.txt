import pandas as pd

def sales_analysis(sales: pd.DataFrame) -> pd.DataFrame:
    return sales.sort_values(by='year').drop_duplicates(subset=['product_id'])[['product_id','year','quantity','price']].rename(columns={'year':'first_year'})
