import pandas as pd

def sales_analysis(sales: pd.DataFrame) -> pd.DataFrame:
    return sales.groupby('product_id')['year'].min().reset_index().merge(sales).rename(columns={'year':'first_year'}).iloc[:,[0,1,3,4]]
